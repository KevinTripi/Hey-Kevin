import 'package:flutter/material.dart';

class GptDatabase extends StatelessWidget {
  const GptDatabase({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          "Bruh",
          style: TextStyle(
              backgroundColor: Colors.white,
              color: Colors.black,
              fontSize: 100),
        ),
      ),
    );
  }
}
